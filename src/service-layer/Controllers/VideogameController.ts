import { Request, Response } from 'express';
import AddVGTask, { AddVGData} from '../Task/AddVGTask';
import BaseController from './BaseController';

export default class VideogameController extends BaseController {
  public constructor() {
    super('/VGs');
  }









}